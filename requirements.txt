# Core detector uses only the Python standard library (Python 3.9+).
# Optional: domain-age lookup
python-whois>=0.9.4
# Optional: ML experiment (ml/train.py)
scikit-learn>=1.3
joblib>=1.3
# Development
pytest>=7.4
